import { Action } from '@reduxjs/toolkit';

const INITIAL_STATE = {};

export const postsReducer = (state = INITIAL_STATE, action: Action) => {
  switch (action.type) {

    default:
      return state;
  }
};
